name: build-and-smoke-test

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x
      - run: dotnet build ImmichUploaderApp.sln -c Release
      - run: dotnet run --project tests\ImmichUploaderApp.SmokeTests\ImmichUploaderApp.SmokeTests.csproj -c Release
